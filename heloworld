#include <stdio.h>
int main()
{
   // printf() displays the string inside quotation
   printf("Hello!");
   int a,b; // a and b are termed as variables with type int that is nothing but integer . So now we have 2 have variables a and b for basic addition operation

printf(“Enter the 2 numbers”);// printf is used to print something on the screen

scanf(“%d%d”,&a,&b);// scanf is used to read the data. You can refer this for %d What does %d mean?

printf(“%d”,a+b);

return 0;// As we have used int with main so for successful return
}
